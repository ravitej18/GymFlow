# Local planning/export files
plan.doc
plan.md

# Firebase Admin SDK/service account keys. Never commit private keys.
*-firebase-adminsdk-*.json
*service-account*.json
firebase-admin*.json

# Local environment files
.env
.env.*

# OS/editor noise
.DS_Store
Thumbs.db

# Kilo config and GSD framework
.kilo/
.agents/

# Temporary developer seeding utilities
seed.html
database_scalability_report.md

# Dependencies & Node modules
node_modules/
/package.json
/package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Capacitor native shell. The config, sync script and docs under native/ are
# tracked; the copied web bundle and generated platform project are not.
native/www/
native/android/
native/node_modules/
native/package-lock.json
