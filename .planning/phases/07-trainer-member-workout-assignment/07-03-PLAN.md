---
phase: 07-trainer-member-workout-assignment
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - modules/my-workout.js
  - modules/trainers.js
  - app.js
  - scripts/smoke-test.mjs
autonomous: true
requirements:
  - P7-SC4
  - P7-SC5
  - P7-SC6
user_setup: []

must_haves:
  truths:
    - "A signed-in member can open a 'My Workout' screen that shows today's trainer-authored session when one exists for today"
    - "When no session exists for today but a template is assigned, the member sees the currently assigned template's exercises (fallback)"
    - "When neither a session nor an assignment exists, the member sees an empty state"
    - "The member can view their full assignment history (each past template assignment with its date), newest first"
    - "The owner sees a read-only overview on the Trainers screen listing every member's current assignment (member → trainer → template), with no edit controls"
  artifacts:
    - path: "modules/my-workout.js"
      provides: "Member-facing My Workout Today screen with session→template fallback + assignment history"
      exports: ["myWorkoutModule"]
      min_lines: 70
    - path: "modules/trainers.js"
      provides: "Owner read-only assignment overview section appended below the trainer card grid"
      contains: "Assignment"
    - path: "app.js"
      provides: "my-workout route registered (import, modules map, nav entry, role=member)"
      contains: "my-workout"
    - path: "scripts/smoke-test.mjs"
      provides: "myWorkoutModule covered in member + member-pending render paths"
      contains: "myWorkoutModule"
  key_links:
    - from: "modules/my-workout.js"
      to: "workout_sessions collection"
      via: "find session by memberId === myMember.id AND date === today()"
      pattern: "date === today\\(\\)"
    - from: "modules/my-workout.js"
      to: "workout_assignments collection"
      via: "filter by memberId, sort by assignedAt desc for current + history"
      pattern: "memberId === "
    - from: "modules/trainers.js owner overview"
      to: "context.data.workout_assignments"
      via: "render member→trainer→template rows read-only"
      pattern: "workout_assignments"
    - from: "app.js modules map + nav"
      to: "modules/my-workout.js"
      via: "import myWorkoutModule and register route my-workout for role member"
      pattern: "myWorkoutModule"
---

<objective>
Create the member-facing `modules/my-workout.js` ("My Workout", member-only) and add a read-only
owner assignment-overview section to `modules/trainers.js`. The member screen shows today's
trainer-authored session if one exists, otherwise falls back to the currently assigned template, and
lists the member's full assignment history. The owner section lists every member's current
assignment (member → trainer → template) with no edit controls.

Purpose: Delivers Phase 7 success criteria 4 (member sees today's session or assigned template),
5 (member assignment history), and 6 (owner assignment overview). This is the consumer side of the
trainer→member workflow — it reads the `workout_sessions` and `workout_assignments` records that
07-02 produces.

Output: New `modules/my-workout.js` exporting `myWorkoutModule`; owner overview section in
`modules/trainers.js`; `app.js` wired (import, modules map, nav entry); `scripts/smoke-test.mjs`
covers the new member module for member and member-pending contexts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-trainer-member-workout-assignment/07-RESEARCH.md
@modules/my-membership.js
@modules/trainers.js
@modules/workouts.js
@modules/utils.js
</context>

<artifacts_produced>
Symbols and files this plan creates or modifies:
- `modules/my-workout.js` (NEW): exports `const myWorkoutModule = { render(context) }` (no `bind` needed — read-only screen).
- `modules/trainers.js`: appends a read-only assignment-overview `<section class="panel">` below the existing `.work-grid`; no new exports, render function returns the existing markup plus the new section.
- `app.js`: adds `import { myWorkoutModule } from "./modules/my-workout.js";`; adds `"my-workout": myWorkoutModule` to the `modules` registry; adds nav entry `["my-workout", "My Workout", "fitness_center", ["member"]]` to the `nav` array.
- `scripts/smoke-test.mjs`: adds `import { myWorkoutModule } from "../modules/my-workout.js";` and adds `myWorkoutModule` to the `memberModules` array.

Reads collections registered in 07-01: `workout_sessions`, `workout_assignments`, `workout_templates`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create modules/my-workout.js (today's session → template fallback + history)</name>
  <files>modules/my-workout.js</files>
  <read_first>
    - modules/my-membership.js (the canonical member-role read-only module: `render(context)` with an
      early-return empty state when `context.myMember` is falsy, `.metric-grid`, `.panel.stack`,
      `.panel-heading`, `.detail-grid`; note this module has NO `bind`)
    - modules/workouts.js (how template `exercises` are rendered inside a `<pre>` block with `escapeHtml`)
    - modules/utils.js (helpers to import: `dateLabel`, `emptyState`, `escapeHtml`, `findName`,
      `pageHeader`, `today`)
    - .planning/phases/07-trainer-member-workout-assignment/07-RESEARCH.md ("New Collections Design"
      keying strategy: today's session = `s.memberId === myMember.id && s.date === today()`; current
      assignment = latest by `assignedAt`)
  </read_first>
  <action>
    Create `modules/my-workout.js` exporting a single `myWorkoutModule` object with only
    `render(context)` (no `bind` — this screen is read-only, mirroring `my-membership.js`).

    Imports: `dateLabel, emptyState, escapeHtml, findName, pageHeader, today` from `./utils.js`.

    render(context):
    - `const me = context.myMember;` If falsy, return `pageHeader("My Workout")` followed by
      `emptyState("Membership being set up", "Your workout will appear here once your gym finalises your membership.")`.
    - `const templates = context.data.workout_templates || [];`
    - `const sessions = context.data.workout_sessions || [];`
    - `const assignments = context.data.workout_assignments || [];`
    - Today's session: `const todaySession = sessions.find(s => s.memberId === me.id && s.date === today());`
    - Member's assignments newest-first:
      `const myAssignments = assignments.filter(a => a.memberId === me.id).sort((a, b) => String(b.assignedAt || "").localeCompare(String(a.assignedAt || "")));`
      `const current = myAssignments[0] || null;`
    - Build the "Today" panel (`<section class="panel stack">`, heading `<h2>Today's Workout</h2>`):
      - If `todaySession`: show `<small>Session for ${dateLabel(todaySession.date)}</small>`, the base
        template name when present via `findName(templates, todaySession.templateId, "")`, the exercises
        in a `<pre>${escapeHtml(todaySession.exercises || "No exercises listed")}</pre>`, and the notes
        in a `<small>${escapeHtml(todaySession.notes || "")}</small>`.
      - Else if `current`: render the FALLBACK — the currently assigned template. Look up the template
        by `current.templateId`; show its name via `findName(templates, current.templateId, "Assigned template")`,
        a hint line indicating this is the assigned plan (e.g. `<small>No session written for today — showing your assigned plan.</small>`),
        and the template's `exercises` in a `<pre>` (find the template object in `templates` by id to
        read its `exercises` string; escape it).
      - Else: render `emptyState("No workout assigned yet", "Your trainer hasn't assigned a workout plan or written a session yet.")`.
    - Build the "Assignment History" panel (`<section class="panel">`, heading
      `<h2>Assignment History</h2>` with a count `<span>${myAssignments.length} assignments</span>`):
      - If `myAssignments.length`, render a `.data-table` with a `.table-head` of `<span>Date</span><span>Template</span>`
        and one `.table-row` per assignment (inline `style="grid-template-columns:1fr 2fr"`) showing
        `dateLabel(a.assignedAt)` and `escapeHtml(findName(templates, a.templateId, "—"))`.
      - Else render `emptyState("No history yet", "Template assignments will be listed here.")`.
    - Wrap both panels in a `pageHeader("My Workout")` + `.work-grid` container (or stack — match the
      member-facing layout style of `my-membership.js`). Escape every interpolated string with
      `escapeHtml`; never hardcode colours.
  </action>
  <verify>
    <automated>node -e "import('./modules/my-workout.js').then(m=>{const t=today=>new Date().toISOString().slice(0,10);const d=new Date().toISOString().slice(0,10);const withSession=m.myWorkoutModule.render({myMember:{id:'m1'},data:{workout_sessions:[{memberId:'m1',date:d,exercises:'Bench 3x10'}],workout_assignments:[],workout_templates:[]}});if(!withSession.includes('Bench 3x10'))process.exit(1);const fallback=m.myWorkoutModule.render({myMember:{id:'m1'},data:{workout_sessions:[],workout_assignments:[{memberId:'m1',assignedAt:'2026-01-01',templateId:'w1'}],workout_templates:[{id:'w1',name:'PPL',exercises:'Squat 5x5'}]}});if(!fallback.includes('Squat 5x5'))process.exit(1);const empty=m.myWorkoutModule.render({myMember:{id:'m1'},data:{workout_sessions:[],workout_assignments:[],workout_templates:[]}});if(!empty.includes('No workout assigned yet'))process.exit(1);const pending=m.myWorkoutModule.render({myMember:null,data:{}});if(!pending.includes('Membership being set up'))process.exit(1);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const myWorkoutModule' modules/my-workout.js` returns 1
    - Module contains `s.date === today()` (today's-session match)
    - Module contains `a.memberId === me.id` and a descending sort on `assignedAt` (current + history)
    - The node verify command prints `ok`, proving all four paths: (a) today's session shown when present, (b) assigned-template fallback shown when no session, (c) `No workout assigned yet` empty state when neither, (d) `Membership being set up` when `myMember` is null
    - Module does NOT export a `bind` function (read-only screen): `grep -c 'bind' modules/my-workout.js` returns 0
  </acceptance_criteria>
  <done>
    `modules/my-workout.js` renders today's session, the assigned-template fallback, the no-workout
    empty state, and the full assignment history — all read-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add read-only owner assignment overview to modules/trainers.js</name>
  <files>modules/trainers.js</files>
  <read_first>
    - modules/trainers.js (the existing `render({ data })` returning `pageHeader("Trainers")` + a
      `.work-grid`; the `card(trainer)` helper; the import line pulling helpers from `./utils.js`)
    - modules/utils.js (helpers already imported here: `escapeHtml`, `findName`; the `.data-table`
      pattern and `dateLabel` are also available)
    - .planning/phases/07-trainer-member-workout-assignment/07-RESEARCH.md ("Owner view: read-only, no
      override" decision — purely informational, no save/edit)
  </read_first>
  <action>
    Extend `modules/trainers.js` to append a read-only assignment-overview section below the existing
    `.work-grid` in `render`. Do NOT change the trainer CRUD form, the `card` helper, or the existing
    `bind` handlers.

    In the import line at the top, add `dateLabel` to the helpers imported from `./utils.js` if not
    already present (the file already imports `findName`-adjacent helpers; add only what is missing —
    do not remove existing imports).

    In `render({ data })`:
    - Compute the current assignment per member from `data.workout_assignments || []`: group by
      `memberId`, and for each member keep the assignment with the latest `assignedAt` (sort the
      member's assignments by `String(b.assignedAt||"").localeCompare(String(a.assignedAt||""))`, take
      `[0]`). Reference `data.members`, `data.trainers`, `data.workout_templates` for display names.
    - Append a new `<section class="panel">` AFTER the closing `</div>` of the `.work-grid`, with a
      `panel-heading` `<h2>Workout Assignments</h2>` and a count span of the number of members with a
      current assignment.
    - If there is at least one current assignment, render a `.data-table` with a `.table-head` of
      `<span>Member</span><span>Trainer</span><span>Template</span><span>Assigned</span>` and one
      `.table-row` per current assignment (inline `style="grid-template-columns:1fr 1fr 1fr auto"`):
      member name via `findName(data.members, a.memberId, "—")`, trainer name via
      `findName(data.trainers, a.trainerId, "—")`, template name via
      `findName(data.workout_templates, a.templateId, "—")`, and `dateLabel(a.assignedAt)`.
      Every value wrapped with `escapeHtml` where it is a name string.
    - If there are no assignments, render an empty state (`emptyState("No assignments yet", "Trainers' workout assignments will appear here.")`) inside the section.
    - The section MUST contain no `<button>`, no `data-*` action attribute, and no `<form>` — it is
      read-only (owner cannot override trainer assignments).
  </action>
  <verify>
    <automated>node -e "import('./modules/trainers.js').then(m=>{const html=m.trainersModule.render({data:{trainers:[{id:'t1',name:'T'}],members:[{id:'m1',fullName:'M'}],workout_templates:[{id:'w1',name:'PPL'}],workout_assignments:[{memberId:'m1',trainerId:'t1',templateId:'w1',assignedAt:'2026-06-01'}]}});if(!html.includes('Workout Assignments')||!html.includes('PPL'))process.exit(1);console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `modules/trainers.js` render output contains the literal heading text `Workout Assignments`
    - The node verify command prints `ok`: the owner render includes `Workout Assignments` and the template name `PPL` for a member with one assignment
    - The new section markup contains no edit affordance — verified by reading the file: the
      assignment-overview `<section>` has no `<button` and no `data-edit`/`data-approve`/`data-assign`
      attribute and no `<form` inside it
    - `node scripts/smoke-test.mjs` still exits 0 (trainers module already in `ownerModules`)
  </acceptance_criteria>
  <done>
    The Trainers screen shows a read-only Workout Assignments table (member → trainer → template →
    date) with no edit controls; existing trainer CRUD is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register my-workout route in app.js and smoke-test.mjs</name>
  <files>app.js, scripts/smoke-test.mjs</files>
  <read_first>
    - app.js (the import block, the `nav` array, the `modules` registry — observe how `my-membership`
      is imported, registered, and given role `["member"]`)
    - scripts/smoke-test.mjs (the import block, the `memberModules` array, and the member render loops
      against `memberContext` and `pendingMember`)
  </read_first>
  <action>
    In `app.js`:
    - Add `import { myWorkoutModule } from "./modules/my-workout.js";` to the module import block.
    - Add the nav entry `["my-workout", "My Workout", "fitness_center", ["member"]]` to the `nav` array
      (place it near the other member entries such as `my-membership` / `my-payments`). The fourth
      element MUST be exactly `["member"]`.
    - Add `"my-workout": myWorkoutModule` to the `modules` registry object.
    Do not change routing logic or unrelated entries.

    In `scripts/smoke-test.mjs`:
    - Add `import { myWorkoutModule } from "../modules/my-workout.js";` to the import block.
    - Add `myWorkoutModule` to the `memberModules` array so it renders against both `memberContext`
      (member with a roster doc) and `pendingMember` (`myMember: null`). The module must not crash when
      `data.workout_sessions` / `data.workout_assignments` / `data.workout_templates` are empty arrays
      (present in `makeData()` after 07-01).
  </action>
  <verify>
    <automated>node scripts/smoke-test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { myWorkoutModule }' app.js` returns 1
    - `grep -c '"my-workout": myWorkoutModule' app.js` returns 1
    - `grep -c '\["my-workout", "My Workout", "fitness_center", \["member"\]\]' app.js` returns 1
    - `grep -c 'import { myWorkoutModule }' scripts/smoke-test.mjs` returns 1
    - `scripts/smoke-test.mjs` `memberModules` array includes `myWorkoutModule`
    - `node scripts/smoke-test.mjs` exits 0 and prints `Smoke render passed (owner + member + trainer + pending).`
  </acceptance_criteria>
  <done>
    The `my-workout` route is member-scoped, importable, and renders without error in both member and
    member-pending smoke contexts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered HTML → DOM | Session exercises/notes, template names, and member names are interpolated into innerHTML |
| member client → Firestore read | Member reads workout_sessions / workout_assignments (gymId-scoped by rules + client list filter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering (XSS) | exercises/notes/name rendering in my-workout + owner table | mitigate | All interpolated string fields wrapped in `escapeHtml()`, exercises rendered inside `<pre>` with escaping, matching `workouts.js` |
| T-07-09 | Information Disclosure | member seeing another member's session | mitigate | Client filters by `memberId === myMember.id`; Firestore read rule scopes to same gym (07-01) |
| T-07-10 | Elevation of Privilege | owner overriding trainer assignment | mitigate | Owner section is read-only — no button/form/data-action; write path does not exist in this UI |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure ES module, zero new dependencies |
</threat_model>

<verification>
- `node scripts/smoke-test.mjs` passes including the new member module in member and pending paths
- Inline node render checks confirm: today's-session display, assigned-template fallback, no-workout
  empty state, member-pending empty state, and the owner read-only assignment table
- `grep` confirms the session match (`date === today()`), the member-scoped assignment filter, the
  absence of a `bind` in my-workout, and the absence of edit controls in the owner overview section
- Manual UAT (deferred to phase verification): member with a today session sees it; member with only
  an assignment sees the template; member with neither sees the empty state; owner sees the read-only
  assignment table on the Trainers screen
</verification>

<success_criteria>
- Member "My Workout" shows today's trainer session when present, else the assigned template, else an empty state
- Member can view full assignment history newest-first
- Owner sees a read-only assignment overview (member → trainer → template → date) on the Trainers screen
- Both new routes are role-scoped and smoke test green
</success_criteria>

<output>
Create `.planning/phases/07-trainer-member-workout-assignment/07-03-SUMMARY.md` when done
</output>
